apiVersion: v1
kind: Service
metadata:
  name: todoapp-servise
  namespace: todoapp
spec:
  selector:
    app: todoapp-12
  ports:
  - protocol: TCP
    port: 80
    targetPort: 8080
  type: ClusterIP
